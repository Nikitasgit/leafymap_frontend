import { Roboto, SUSE } from "next/font/google";

//Import Google Fonts
export const roboto = Roboto({
  subsets: ["latin"],
  weight: ["400", "500", "600", "700"],
});

export const suse = SUSE({
  subsets: ["latin"],
  weight: ["400", "500", "600", "700"],
});
