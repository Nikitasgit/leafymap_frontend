/**
 * Fichier temporaire pour vérifier que GitHub Actions bloque bien
 * quand le build échoue. À supprimer après validation.
 */
export const ciFailureCheck = (): number => {
  return "this should be a number";
};
